# Linux-Router-Configuration-Lab
This lab demonstrates how to configure a Linux system inside a virtual machine to function as a Layer 3 router.  The objective was to simulate two isolated networks and manually configure routing between them without using GUI tools or network simulators.
